# Database connection (provided by Netlify DB after `npx netlify db init`)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# NeonAuth URL (find in Neon Console > Auth tab after enabling Auth)
NEON_AUTH_URL=https://your-project-id.auth.neon.tech
