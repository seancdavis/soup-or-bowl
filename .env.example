# Database connection (provided by Netlify DB after `npx netlify db init`)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# NeonAuth (provided by Neon console after enabling Auth)
# NEON_AUTH_URL=https://your-project.neon.tech/auth
